apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: test
resources:
  - podinfo-test.yaml
secretGenerator:
  - name: wildcard-cert
    namespace: test
    literals:
      - tls.crt="${tls.crt}"
      - tls.key="${tls.key}"
    type: "kubernetes.io/tls"
configurations:
  - kustomizationconfig.yaml